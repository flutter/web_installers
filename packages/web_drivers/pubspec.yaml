name: web_driver_installer
description: For downloading/installing different web drivers. Used in tests.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  args: ">=1.5.2 <3.0.0"
  http: ">=0.12.0 <1.0.0"
  path: ">=1.6.4 <2.0.0"
  test: ">=1.6.5 <2.0.0"
  yaml: ">=3.0.0 <4.0.0"
